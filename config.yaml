# Nexus Multi-Agent Platform Configuration
# Edit this file to customize agent behavior.

project_name: nexus
default_provider: openai
default_model: mimo-v2.5-pro

# Agent defaults
max_iterations: 30
max_concurrent_agents: 3

# LLM providers
providers:
  openai:
    base_url: https://token-plan-cn.xiaomimimo.com/v1
    api_key: tp-cm7o8wbehuvdpx6cshelf703negzh7qhmvudrw0mmelsr3ze

# Research tools (future)
# arxiv:
#   max_results: 10
#
# web_search:
#   engine: brave
#   api_key: ${BRAVE_API_KEY}

# Visualization
visualizer:
  enabled: true
  port: 8080
